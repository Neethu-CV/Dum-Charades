name: Dum Charades - Flask Deployment

on:
  push:
    branches: 
      - master
      - main
  pull_request:
    branches: 
      - master
      - main
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.12.2

    - name: Install Python Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirement.txt

    - name: Deploy Flask Application
      run: |
        echo "Your Flask app is ready to deploy."
        # Add deployment steps here (e.g., deploy to Heroku, AWS, or any hosting service).
